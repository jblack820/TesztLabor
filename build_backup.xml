<?xml version="1.0" encoding="UTF-8"?><!-- You may freely edit this file. See commented blocks below for --><!-- some examples of how to customize the build. --><!-- (If you delete it and reopen the project it will be recreated.) --><!-- By default, only the Clean and Build commands use this build script. -->
<project name="TestCenterFX" default="default" basedir="." xmlns:fx="javafx:com.sun.javafx.tools.ant">
    <description>Builds, tests, and runs the project TestCenterFX.</description>
    <import file="nbproject/build-impl.xml"/>

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="**/*.jar"/>
    </path>

  <property name="JAVA_HOME" value="C:\\Program Files\\Java\\openlogic-openjdk-8u292-b10-windows-64"/>
  <property name="build.src.dir" value="src"/>
  <property name="build.classes.dir" value="classes"/>
  <property name="build.dist.dir" value="dist"/>
  <property name="lib.dir"     value="lib"/>

  <target name="default" depends="clean,compile">
    <taskdef resource="com/sun/javafx/tools/ant/antlib.xml"
      uri="javafx:com.sun.javafx.tools.ant"
      classpath="${JAVA_HOME}/lib/ant-javafx.jar"/>

      <fx:application id="TestCenterFX_1.5"
        name="TestCenterFX_1.5"
        mainClass="Main"/>

      <fx:resources id="appRes">
        <fx:fileset dir="${build.dist.dir}" includes="TestCenterFX_1.5.jar"/>

      </fx:resources>

      <fx:jar destfile="${build.dist.dir}/TestLab.jar">
        <fx:application refid="TestCenterFX_1.5"/>
        <fx:resources refid="appRes"/>
        <fileset dir="${build.classes.dir}"/>
      </fx:jar>

      <fx:deploy width="300" height="250"
        nativeBundles="all"
        outdir="." embedJNLP="true"
        outfile="testlab">

        <fx:application refId="TestCenterFX_1.5"/>

        <fx:resources refid="appRes"/>

        <fx:info title="TestLab Application"
          vendor="TGP"/>

      </fx:deploy>
  </target>

  <target name="clean">
    <mkdir dir="${build.classes.dir}"/>
    <mkdir dir="${build.dist.dir}"/>

    <delete>
      <fileset dir="${build.classes.dir}" includes="**/*"/>
      <fileset dir="${build.dist.dir}" includes="**/*"/>
    </delete>

  </target>

  <target name="compile" depends="clean">
    <javac includeantruntime="false"
      srcdir="${build.src.dir}"
      destdir="${build.classes.dir}"
      classpathref="classpath"
      fork="yes"
      encoding="UTF-8"
      executable="${JAVA_HOME}/bin/javac"
      source="1.8"
      debug="on">
    </javac>
  </target>

  <target name="run" depends="jar">
          <java fork="true" classname="${main-class}">
              <classpath>
                  <path refid="classpath"/>
                  <path location="${jar.dir}/${ant.project.name}.jar"/>
              </classpath>
          </java>
      </target>



</project>
